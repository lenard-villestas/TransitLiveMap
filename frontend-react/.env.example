# Copy to .env and paste your free MapTiler key (https://www.maptiler.com/).
# If left blank, the app falls back to the keyless OpenFreeMap basemap so it
# still runs out of the box.
VITE_MAPTILER_KEY=
